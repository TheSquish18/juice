/// @description Insert description here
// You can write your code in this editor

total = instance_number(obj_balls);

if (total <= 5) {
	instance_create_layer(x, y, "Instances", obj_balls);
	instance_create_layer(x, y, "Instances", obj_balls);
	instance_create_layer(x, y, "Instances", obj_balls);
	instance_create_layer(x, y, "Instances", obj_balls);
	instance_create_layer(x, y, "Instances", obj_balls);
	
}

